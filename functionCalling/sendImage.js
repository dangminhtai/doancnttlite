export function sendImage(send_image) {
    if (send_image) {
        // Thực thi hàm gửi ảnh cho người dùng và trả về giao diện Discord
        //Demo đơn giản
        console.log('Bot đã gửi ảnh thành công!!');
    }

}